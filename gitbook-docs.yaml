$schema: https://api.gitbook.com/gitbook-docs.yaml

site:
  title: MDE-Internals
  structure:
    - type: space
      key: mde-internals
      title: MDE-Internals
      path: /
      default: true
      content:
        directory: ./
